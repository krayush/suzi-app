import { Text } from "react-native";

export default function Layout() {
  return (
    <Text>
      From the docs - "Layout files in a directory are used to define shared UI
      elements such as headers, tab bars so that they persist between different
      routes."
    </Text>
  );
}
