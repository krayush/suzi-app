import { Text } from "react-native";

export default function Routing() {
  return (
    <Text>
      <Text>[user].tsx</Text>: This is used for dynamic routes
      <Text>
        Expo Router uses a stack-based navigation model, where each new route
        you navigate to is added to a stack.
      </Text>
    </Text>
  );
}
