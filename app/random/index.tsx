import { Text, View } from "react-native";

export default function Random() {
  return (
    <View style={{ gap: 8 }}>
      <Text>
        scheme in app.json is used for deep linking (allowing a specific screen
        to be opened directly)
      </Text>
    </View>
  );
}
